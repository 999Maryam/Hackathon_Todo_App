apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: no-mtls-config
  namespace: default
spec:
  tracing:
    samplingRate: "0"
    zipkin:
      endpointAddress: ""
  metric:
    enabled: true
  httpPipeline:
    handlers: []
  features:
  - name: InputBindingErrorAsNotFound
    enabled: true